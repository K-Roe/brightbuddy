import { Text, View } from "react-native";

export default function Routine() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center"
      }}
    >
      <Text style={{ fontSize: 28 }}>Routine</Text>
    </View>
  );
}
